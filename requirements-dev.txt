# CI 与本机共用的工具链版本 —— 单一事实源。
# 为什么只有 ruff 锁死到补丁版本, 其余只封主版本:
#   lint 工具会对"没改过的代码"给出不同结论 (ruff 0.16 扩大了默认规则集, 导致 W1 收尾时
#   本机绿、CI 红), 所以必须精确锁死; 测试框架不会凭空判定代码有问题, 封住主版本
#   足以挡掉破坏性变更, 同时不强迫每台开发机同步升级。
# 取舍全文见 docs/adr/ADR-005-pin-lint-toolchain.md。
ruff==0.16.1
pytest>=8,<10
pydantic>=2.8,<3
PyYAML>=6,<7
