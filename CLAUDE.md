# Sentinel — AI Coding 约定

## 架构不变量 (违反即 review 打回)
1. **LLM 只能产出可撤销、只在系统内部留痕的东西** (Policy 草案 / 报告草稿 /
   一条"请人来看一眼"的审批请求)。**不可撤销、外部世界看得见的动作** ——
   发布上线、真的发邮件、真的点亮现场的灯 —— 一律在它的能力之外。
   其中 publish **由数据库外键强制**: 没有 approvals 记录, `policy_publications`
   那一行在物理上插不进去 (ADR-007), 与应用代码写成什么样无关。
   (W4 改准了措辞: 原文写"永远没有副作用执行权限", 而 Agent 的
   `create_policy` / `request_approval` 本来就要写库 —— 按字面读, 这条被自己的
   工具清单打脸。改后是一句可以当场验证的事实, 而不是一句需要别人相信的承诺。
   面试口径与 README 同此, 见 SPEC-002 第十一节。)
2. packages/policy_engine 零 IO: 纯函数吃事件流。执行器与模拟器是同一份 evaluate()。
3. DSL 的能力边界 == 模拟器可验证边界。加能力必须同时提交 schema + 语义校验 + 引擎实现 + 场景包用例。
4. 只有 services/ 层可以碰数据库; Agent tools 与 MCP server 复用同一 service。
5. requires_approval 由服务端从 action 分级推导, 永不信任模型输入。

## 研发流程 (Spec Coding)
- 功能先写 docs/specs/SPEC-xxx.md (半页: 目标/接口/验收), commit message 关联 spec 编号。
- 关键取舍写 docs/adr/, 每篇 ≤1 页。
- AI 生成代码引入的缺陷, 在 docs/ai-development/defect-log.md 记录完整案例。

## 工程约定
- Python 3.12 / ruff / mypy; TS strict。
- mypy 分档: 应用与引擎代码开严格档, 测试默认档; 严格档逐模块打开,
  白名单在 mypy.ini, 新模块补完标注后加入。理由与待补清单见 ADR-005。
- 测试命名: test_<行为>__<条件>。评测 grader 必须确定性, 禁止 LLM judge。
- 文档只用文字与 Markdown 结构表达状态 (完成 / 进行中 / 未开始), 强调用粗体;
  不用表情符号与装饰性图标。

## commit message 的分寸 (这是求职作品集, log 是公开的)

- 主题行一句话说清改了什么; 正文 3–5 行封顶。
- 写**改了什么、为什么、验证到什么程度** —— 变异测试、独立重算、零花费这类
  结论值得写, 它们展示的是纪律。
- **不写内部角色与返工过程**: 不出现"评审方"/"CC 说"/"上一批漏了"/"我判错了"。
  自我批评是这个项目的资产, 但它属于 `defect-log.md` 与 `docs/进度与交接.md` ——
  那些是读者主动点进去看的, log 是被扫的。

## 协作红线

- **git 一律由本人执行**。AI 可以直接写文件、改代码, 但 `git add` / `git commit` /
  `git push` / `git reset` / 任何改动仓库历史的命令, 都由本人在自己终端敲。
  AI 只负责列出"这批改了哪些文件、建议的 commit message", 不代劳执行。
  (背景: 通过文件桥接跑 git 会在 `.git/` 留下 `index.lock` 残留, 导致后续 git 报
  `Unable to create '.git/index.lock': File exists`。)
- 多个 AI 同时改同一个仓库时, **先划清文件边界再动手**。文件桥接是整文件写入,
  没有合并, 谁后写谁赢 —— 覆盖前先比对校验和, 确认对方没在你取快照之后动过。
- **AI 产生的任何文件都必须落在本仓库内**。需要临时脚本就放 `scripts/dev/`,
  不要写进系统临时目录 —— 那些目录重启即清、Finder 里看不见、也不进版本库,
  等于交付了一个随时会消失、且没人复核过的东西。
  (背景: W4 第二段的录制脚本被写进 `/private/tmp/...`, 而它是唯一会真实调用
  模型、真实花钱的那一段代码, 恰恰绕过了复核。)
- **密钥只进 `.env`, 不进任何别的地方**: 不进代码、不进注释、不进录制文件、
  不贴进聊天窗口。key 一旦出现在聊天记录、截图或剪贴板历史里, 就按已泄露处置 ——
  去控制台删掉重建, 而不是"应该没人看到"。往 `.env` 里填 key 用
  `read -rs` 读入 (zsh 下不要用 `read -p`), 让它不上屏、不进终端历史。
