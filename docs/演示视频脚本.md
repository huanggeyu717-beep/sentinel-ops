# 终版演示视频 — 分镜脚本 (60–90 秒, 无口播, 录屏 + 字幕)

> 形式: 屏幕录制, 不录音。字幕为硬字幕 (烧进画面), 每条停留够读完。
> 目标观众: 简历筛选阶段的 HR 与一面面试官 —— **大多数人只看前 20 秒**,
> 所以前 20 秒必须把"这系统是干什么的"讲完, 后面才是机制。
>
> 一句话主张 (整支片子只讲这一句): **模型只写草稿, 数字全由代码给, 不可撤销的动作它碰不到。**

## 录之前

- **窗口** 1280×800, 浏览器缩放 100%, 隐藏书签栏与其它标签页;
- **账号** 用 `chris@example.com` (manager, 看得到审批与报告);
- **数据** 先 `make reset` 再 `make sim`, 让时间线是干净的;
- **深色/浅色** 跟仓库截图保持一致;
- **鼠标** 移动放慢, 每次点击后停 1 秒再动 —— 剪辑时好切;
- **不要**录到 `.env`、终端里的 key、或任何真实邮箱。

## 分镜

| # | 时间 | 画面 | 字幕 (本人定稿 2026-08-24) |
|---|---|---|---|
| 1 | 0:00–0:06 | Dashboard 平面图, 一个传感器由绿转红, 事故卡片弹出 | 生鲜区漏水。传感器报湿, 系统自动开单。 |
| 2 | 0:06–0:12 | 事故详情页, 时间线依次出现 开单 → 派单 → 刷卡接单 | 主管派单, 到场工作人员刷卡接单, 处理完成后关单。 |
| 3 | 0:12–0:20 | Automation Studio, 输入一句人话建策略任务 | 可以输入一句话明确什么情况下应升级、通知谁等规定。 |
| 4 | 0:20–0:30 | Trace 时间线逐步点亮 parsing → discovering → compiling → validating | 时间线显示模型每一步查了什么、写了什么、验证器如何判断。 |
| 5 | 0:30–0:38 | 停在"等待审批", 鼠标悬停在审批按钮上不点 | 发布需要另一个工作人员审批。没有审批记录, 新的规则发布不能插进数据库。 |
| 6 | 0:38–0:46 | 回到已解决的事故, 点"生成报告" | 事故处理完成。点"生成报告", 模型会产出报告。 |
| 7 | 0:46–0:56 | 报告页: 左边占位符原文 `{{ack_by}}` / `{{handle_duration}}`, 右边渲染后的中文 | 左边是模型写的占位符, 右边的数字和人名是代码填进去的。 |
| 8 | 0:56–1:04 | 报告页上那两个计数 (`bare_fact_attempts` / `dangling_ref_attempts`) | 这两个数, 是模型想自己写数字、被挡下来的次数。 |
| 9 | 1:04–1:14 | 终端: `python3 scripts/dev/mutate_spec008_stage2.py` 跑出六条全红 | 故意破坏实现后, 对应的测试必须变红。这几处都进行过验证。 |
| 10 | 1:14–1:22 | evals 面板 / README 的五组消融表 | 消融实验通过五组对照测出来, 去掉一项能力会掉多少成功率。 |
| 11 | 1:22–1:30 | 停在 README 顶部, 仓库地址清晰可见 | 代码、设计文档、经历过的错误, 都保留在这里。 |

**第 8 条留一个可选的加强**: 真实调用跑完后, 如果两个计数不是 0, 可以补半句
"这一次它试了 N 回" —— **具体数字比任何论断都有说服力**。计数为 0 就别提,
为 0 只说明这一跑里它没试 (README 里写了同一条口径)。

## 字幕怎么改才不像别人代笔

上面这版是改过一轮的, **但仍然建议你自己再过一遍**, 理由不是谦虚: 字幕是这支片子
里唯一有"人称"的东西, 而看片的人对代笔很敏感。三条经验:

1. **删掉替观众下结论的句子。** "全过程留痕""每一步都看得见"这类是宣传语,
   不是描述。画面已经在放时间线了, 字幕再说一遍等于不信任观众;
2. **一支片子里"不是 A, 是 B"这种句式最多留一条。** 现在留在镜头 5 和 8,
   如果你觉得还是密, 砍掉镜头 8 那条, 只说"它试图自己写数字的次数记在这里";
3. **能说具体数字就别说形容词。** 镜头 9 与其说"关键机制", 不如直接念出来:
   "六条变异, 六条都红"。

**最像你自己的一版, 通常是你看着画面用嘴说一遍、然后把它打出来。** 上面这版当草稿,
别当定稿。

## 剪辑注意

- **镜头 5 和 7 是全片的重点**, 各多留 1–2 秒; 其余可压;
- 镜头 9 的终端字体调大到能看清"六条全红";
- 超过 90 秒就砍镜头 10 (消融那段 README 里有, 视频里不是必需);
- 结尾**不加**"感谢观看"之类的字幕, 停在仓库地址上即可;
- 全片不用表情符号与装饰性图标, 与文档口径一致。

## 录之前必须先做完的

- SPEC-008 的**并排对照**要先补上那次真实调用 (镜头 7、8 拍的就是它的产物);
- 镜头 5 的"等待审批"要有一条真实的待审策略;
- 若 SPEC-009 第三段 (真开机器) 已完成, 镜头 11 可以改成线上地址; **未完成就录本地**,
  不要在视频里出现一个打不开的域名。
