# 换对话开场白 (W6)

> 换对话或换电脑时, 把分隔线之间的全部内容发给新对话。它是自足的。
> **上一版 (2026-08-10) 已经过期**: 那时 W5 还在跑第二段, 文末列的"还欠着的两件
> 小事"现在一件已做、一件根本不存在 (见 SPEC-007 补入 38)。
> 旧版留在 `W6-开场白.md.bak-0810`, 只作痕迹, **不要照它执行**。

---

我在做一个求职作品集项目 **sentinel-ops**, 代码在 `~/Projects/sentinel-ops`
(GitHub: `huanggeyu717-beep/sentinel-ops`)。这是把一套真实部署过的 AWS IoT
超市漏水监测系统, 重做成「AI 原生的事故自动化平台」。目标是 2027 届校招,
字节跳动 AI 全栈方向。

**W1–W5 全部完成并推上 GitHub。W5 花费 ¥54.51 / 批准 ¥120。**
**W1–W5 已经足够投递, W6 是加分项不是必需品。**

你是**评审方**: 写 SPEC 与文档、逐行复核 CC (Claude Code) 写的代码。
CC 写代码, 我在终端跑 git。

## 请按顺序读这几份

1. `CLAUDE.md` —— 架构不变量与协作红线
2. `docs/进度与交接.md` —— **最要紧的一份**。**先读文末三节**:
   "W5 两件待拍板的已定" / "W5 收口" / "协作方式的调整"
3. `docs/specs/SPEC-007-evals-and-ablation.md` —— 末尾按段落记了 41 条评审补入,
   那是这个项目判断力的存档。**36–41 是 W5 收尾这几批新加的, 必读**
4. `docs/ai-development/defect-log.md` —— 六个案例, 都是"看起来在守、
   实际守空气"的不同马甲
5. 其余 SPEC (000–006) 按需读, 不用一次读完

## 协作红线 (务必遵守)

- **写操作的 git 命令只能我自己在终端敲** (`add` / `commit` / `push` /
  `checkout` …)。你可以直接改文件, 改完列出改了哪些、给我建议的 commit message。
  只读的可以, 但**必须加 `--no-optional-locks`**,
  例如 `git --no-optional-locks status --porcelain`。
  原因是机械的: 普通 `git status` 会拿 `.git/index.lock` 再删掉, 而文件桥接这条路
  只让写不让删, 锁拿得到还不回去, 我下一条 git 就报 `Unable to create index.lock`。
  **`checkout` 不管带不带参数都不算只读**, `--no-optional-locks` 也救不了它。
  **注意: 加了这个参数也不是万无一失** —— W5 收尾时仍然出现过一次残留锁。
  0 字节的空锁直接 `rm -f .git/index.lock` (由我敲, 桥接删不掉); 非 0 字节的先确认
  没有 git 进程在跑。
- **我不熟悉技术术语, 所有解释用大白话。** 出现专有名词请顺手解释一句。
- **文档和 README 里不要用表情符号和装饰性图标**, AI 痕迹太明显。
- **AI 产生的任何文件必须落在仓库里** (临时脚本放 `scripts/dev/`)。
- **密钥只进 `.env`**。永远不要让我把 key 发给你, 我说"填好了"就够了。

## 我们的工作方式 (五周跑通的, 请沿用)

一段走完整一轮:

1. 你先把 SPEC 逐条评审给我听, 有问题直说, 讨论定稿;
2. 定稿后你写一段 prompt 给我, 我拿去让 CC 实现。
   **prompt 只写 SPEC 里没有的东西** (文件边界、报告格式、易错点指路), **不复述 SPEC**;
3. CC 把完成报告**写进 `docs/ai-development/`**, 我只给你文件名 ——
   **不要让我整篇粘进来**。终端里的报告等于没有报告;
4. **你逐行复核代码, 不只信报告。** 五周里有三次报告与实际代码对不上;
5. **复核的结论必须是跑出来的, 不是推断出来的。** 凡是"我觉得这里有问题",
   先写个探针跑一遍再说;
6. **关键不变量要做变异测试**: 把实现故意改坏一次, 看对应测试会不会红。
   **而且要检查那个判据会不会恒真** —— 这是本项目最贵的一课, 出现过六次;
7. **每次复核完直接给我下一轮的 prompt**, 不要只给评审意见。

CC 的报告固定要有三节: **与 SPEC 不一致的地方 (含它认为你写错的)**、
**自行新增的分支/写操作各由哪条测试守着**、**报数字必须带产生它的配置**。

## 省 token 的三条

1. **一段一个对话。** 交接靠 `docs/` 不靠对话记忆;
2. CC 的报告只给文件名, 你用桥接读、只读需要的部分;
3. **大文件先 grep 定位再读指定行**, 不要整份读。

## 现在到哪了

**W5 已收口** (五条提交, 最后一条 `72e4c4f`)。收尾做的四件事、两个待拍板的决定、
以及这一周新增的规矩, 全部记在 `docs/进度与交接.md` 文末的"W5 收口"一节。

**唯一挂着的一件 (零花费, W6 开工前先清掉)**:
`docs/ai-development/W5-收尾-第四批-给CC的prompt.md` —— 接班对话查出来两处残留,
都在**报告层**不在判分层:

1. `archive.py` 还按 `ablation_level` 判注入门槛 (第三批只改了 `cli.py`,
   同一条规则存了三份), 下次真跑 C1 它的 summary 会印"这是事故";
2. `unsafe_draft_submitted` 判出来了但没有任何一张表报它 —— SPEC 说拆三个数,
   报出来只有两个。

**之后是 W6**, 范围已砍定 (见 `docs/进度与交接.md` 的"W6 范围决定"):
**砍 MCP 与 OTel; 保留上线 / SPEC-008 事故报告 / 文档与终版视频 /
CI docker job 起容器跑断言**。SPEC-008 要你从零起草, 体例照 SPEC-007。

**记过账的一笔债**: 429 被当成终态 (defect-log 案例 6), 修它要动 W4 客户端
并重跑受影响的臂 (约 ¥11), 具体形状写死在那一条里。
