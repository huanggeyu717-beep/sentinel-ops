# W5 收尾 第五批 · 完成报告

两件事都做完了: **一、渲染器给旧判据归档加了指路句** (得逞率行后紧跟一句
"本行按补入 36 之前的口径判定; 按现行口径的离线重判见
`evals/runs/injection_regrade_v2.json`。", 新旧的判别复用 metrics 的
`unsafe_draft_submitted is None`, archive.py 没有第二份判定条件);
**二、15 份归档的 summary.md 全部按现行渲染器重印**, 四句作废判语
("这是事故不是分数") 全部改正, README 指过去的定形 C1 (20260811-003755) 的
2/10 与 1/10 两个数之间现在有指路句接上了。
**零真实调用、零花费、没有改任何 results.jsonl / manifest.json** ——
重印前后逐份 sha256 核对过, 15 份归档只有 summary.md 变了。

改了 2 个文件, 新建 2 个 (外加 15 份重印的 summary.md):

```
 M evals/runner/archive.py        指路句 (条件 = metrics 报 None) + rerender_summary() 纯函数
 M evals/runner/tests/test_runner_archive_summary.py   3 条新测试 (指路句两方向 + 重渲染幂等)
 ?? scripts/dev/rerender_summaries.py                  重印驱动 (探针告示头特例见一.2)
 ?? docs/ai-development/W5-收尾-第五批-完成报告.md      本文件
 M evals/runs/*/summary.md (15 份)                     重印产物
```

---

## 一、与 SPEC / prompt 不一致的地方 (含发现)

### 1. diff 里有第四种差异, 按 prompt 的要求先弄清楚了 —— 全部能归因, 数字零变化

prompt 允许的差异是 (a) 门槛措辞、(b) unsafe 行、(c) 指路句。落盘前先做了一轮
不落盘的全量 diff, 发现两类之外的差异, 逐条查 git 历史归因如下:

- **多问措辞与多问率行** (08-10 白天的 8 份: 130256/154502/155429 三份 L0,
  155651-L1, 160000/160934 两份 L2, 161625-C1, 162350-C2): 旧行是
  "- 多问 (追问了 must_include 之外槽位) 的用例数: N", 现行渲染器是
  "clarify 类多问 (...) 的用例数: N" 外加一行 "多问率: 不适用 (本归档早于
  kind 字段...)"。**归因**: 这些行在 squash 提交 `2851015` (08-10 20:36) 里
  就已经是现在的样子, 而这 8 份归档是同一天**更早的工作树状态**印的
  (run_id 13:02–16:23), 之后从未重印。第四批报告第三节已对 155429-L0 记录过
  这条既有漂移 ("与本批无关, 记录在案"), 本批是它的收口。**N 逐份相同**
  (0/0/0/0/2/3/8/2), 只有措辞与新增的"不适用"行。
- **130256-L0 另多两行**: "其中结构性 0 共 24 条" 与 "墙钟最长 5 条"。
  **归因**: 同上 —— 它是当天最早的一跑 (13:02, prompt 里也标了"早于补入 31"),
  这两行同样在 `2851015` 里就有, 是渲染器当天晚些新增的。数字都来自冻结的
  results.jsonl, 不是新算的口径。
- **20260811-003029-L2 (v1.3 开跑前探针) 顶上有一段手写告示头**:
  `0ec0238` 提交的"这一页里的任何数字都不要引用"隔离块, 当时的改法是拿它
  **替换**了正文的 "# Run ..." 标题。它不是渲染产物, 从 manifest + results
  重生不出来, 盲目重印会把这段隔离警告删掉。**处置**: 驱动脚本对这一份特判 ——
  告示头逐字接回, 只重印正文 (正文的变化恰好只有指路句 + unsafe "不适用"
  两行, 得逞率 0/0 无措辞行); 形状不对就整份不动并退出非零, 不留丢了告示头的
  中间态。**这一份请在 commit 前重点过目** —— 告示头里"本文件原样保留"那半句
  在重印后已经不再字面成立 (数字仍一个没变), 要不要顺手改那半句由本人定夺。

判定: 前两类不是"渲染器又漂了", 而是"归档比更多的渲染器改动更老" —— 与
(a)(b) 是同一性质, 且测量数字逐个核对为零变化, 所以继续落盘; 第三类不是渲染
问题, 按上面的特判处理。以上就是"停下来说"的内容 —— git 全部未执行, 15 份
重印是否成立, 最终由本人 review 工作树 diff 后拍板, 不认可任何一份都可以单独
还原。

### 2. 指路句对得逞率为 0 的旧归档也出现

prompt 说"得逞率那一行后面补一句", 没限定得逞率非零。按字面实现: 指路句说明的
是**本行的口径**, 不是本行的值, 0/N 的旧归档同样是旧判据判的 (182626-L2 的
0/10 也补了)。探针那份的 0/0 同理。

### 3. rerender_summary 的输入完全来自 manifest 快照

render_summary 除 rows 外还要 not_run / errored / aborted / cassette_bytes 四个
参数, 全部取自 manifest 里既有的键 (`not_run` / `errored` / `aborted_by_budget` /
`cassette_bytes`, 15 份都有), 不另行计算 —— 重印是对三件套的纯函数, 没有第二个
信息来源。

## 二、新增分支各由哪条测试守着

3 条新测试都在 `evals/runner/tests/test_runner_archive_summary.py` (api 档,
与第四批同文件同理由):

- **旧判据归档有指路句且紧跟得逞率行**:
  `test_regrade_pointer__old_manifest_line_follows_got_through_line` ——
  断言整句逐字出现, 且行号恰好是得逞率行 + 1 (得逞为 0 也要有, 见一.2);
- **新判据归档没有指路句**:
  `test_regrade_pointer__new_manifest_has_no_pointer_line` —— 只测一个方向的话,
  "无条件印指路句"的实现照样绿;
- **重渲染幂等且不碰冻结文件**:
  `test_rerender__idempotent_and_never_writes_frozen_files` —— 同一份归档连跑
  两次: 两次落盘逐字节相同、第一次就收敛 (落盘 == 返回值, 旧占位内容被覆盖
  而不是追加)、manifest.json 与 results.jsonl 前后逐字节不变。

### 变异测试 (五个变异逐个注入、跑、复原)

命令统一为 `.venv/bin/python -m pytest evals/runner/tests/test_runner_archive_summary.py -q`
(9 条):

- **变异 A** — 指路句条件反转 (`is not None`)。红 2 条: 两个方向各自抓住
  (2 failed, 7 passed);
- **变异 B** — 指路句永不出现 (`if False`)。红 1 条: 旧归档方向 (1 failed, 8 passed);
- **变异 C** — 指路句挪到抵抗率行之后 (仍出现, 位置不对)。红 1 条: "紧跟得逞率行"
  的行号断言单独抓住 (1 failed, 8 passed) —— 只断言"有这一句"的测试抓不到它;
- **变异 D** — rerender 追加而不是覆盖 (`open("a")`)。红 1 条: 幂等那条
  (落盘 != 返回值) (1 failed, 8 passed);
- **变异 E** — rerender 顺手回写 manifest.json。红 1 条: 冻结文件逐字节断言
  (1 failed, 8 passed)。

五个变异都已复原, 复原后 `git diff evals/runner/archive.py` 逐行核对过恰好
只剩本批的两处改动, 全绿。

## 三、报出的数字与产生它的配置

本批没有产出任何评测数字。报的数字只有测试、lint 与重印核对的:

- `.venv/bin/python -m pytest evals/tests evals/runner/tests -q` → **278 passed**
  (改前 275: 新增 3 条)。配置: 仓库根执行, 本机 venv,
  PYTHONPATH=packages/policy_engine:packages/scenario:apps/api:.;
- `make lint` (venv 的 ruff + mypy) → ruff 0.16.1 (与 CI 一致) All checks
  passed; mypy Success, **134 source files** (上批 133 + 本批的驱动脚本,
  scripts/dev 在 LINT_TARGETS 里), 仅原有的 annotation-unchecked note;
- **重印核对** (驱动脚本跑三遍):
  - 第一遍落盘 15 份; 第二、三遍后对全部 15 份 summary.md 做 sha256 前后比对,
    **逐字节相同** (真实归档上的幂等, 与测试里 tmp 归档的幂等互为印证);
  - `git status --porcelain` 里 evals/runs/ 下**只有 15 个 summary.md 是 M**,
    没有任何 results.jsonl / manifest.json / mutants.json / fault_injection
    文件被写;
  - 逐份 diff 分类: 12 份恰好落在 (a)(b)(c) 三类 + 一.1 的已归因既有漂移;
    003029 探针 = 告示头逐字保留 + 正文加 (b)(c) 两行; 四句作废判语
    (130256 的 4/10 与三份 C1 的 1/10、1/10、2/10) 全部改为
    "臂 X 不设 0% 硬门槛, 记录并解释, 不回滚不重跑 —— SPEC-007 补入 31/37"。

git 未执行任何命令 (只读命令带 `--no-optional-locks`)。建议 commit message:

```
fix(evals): 归档 summary 按现行渲染器重印, 旧判据归档补指路句 (SPEC-007 补入 31/36/37)

四句与现行 SPEC 打架的作废判语 (A0 档与 C1 臂的"这是事故不是分数") 出自旧
渲染器, 全部重印改正; 旧判据归档 (manifest 无 injection_criteria) 的得逞率行
后补指路句, 指向 injection_regrade_v2.json 的离线重判, 判别复用 metrics 的
None 结论。重印是对冻结三件套的纯函数 (rerender_summary), 15 份归档的
results.jsonl 与 manifest.json 逐字节未动, 连跑三遍输出逐字节相同; 探针归档
的手写隔离告示头逐字保留。3 条新测试, 五个变异各被抓红。
```
