# W5 第二段 第三批 · 给 CC 的 prompt

> CI 那三条做得好, 已复核通过, 不用回头改。这一批是**本段最后一批**。

---

## 主体: 照另一份文件做

**`docs/ai-development/W5-第二段-澄清应答修补-给CC的prompt.md`** ——
v1.3 把 `clarify_answer` 改成按槽位索引的字典 + 重跑 L2 / C1。那一份是完整的,
本文件不复述它的任何内容。

**先读那一份, 再看下面这三条追加。**

---

## 追加一: v1.3 落地后, 实跑验证冒烟还是绿的

你选冒烟子集时避开了会被 v1.3 打掉的用例, 这个前瞻做得对 ——
**但推理正确不等于事实正确**, 这条规矩是你们自己立的。

v1.3 数据集定形之后, **实跑一次 `scripts/ci/test-eval-smoke.sh`**,
确认仍是 **10/10、零 miss、零注入得逞**。

**如果有 miss**: 停下来说, 不要顺手重录 —— 那说明 v1.3 动到了那 10 条的
messages, 而你的前瞻结论里有一条不成立, 那件事本身要先弄清楚。

## 追加二: 给冒烟 cassette 立一条重录规矩

在 `evals/cassettes/smoke/README` 里写死:

> **任何 prompt / 工具 Schema / 模型档位的改动都会让这 10 条全部 miss, 冒烟当场红。
> 那时重录是正确处置 —— 但必须先报预估、由人确认后再跑, 不许因为"CI 红了赶紧弄绿"
> 就静默重录。** 静默重录会让这道门槛退化成"永远能被弄绿的东西",
> 而它存在的意义恰恰是**改动要被人看见**。

顺带在那份 README 里写明: 这批 cassette 来自 **L2 v1.2 那一跑**, 是一个
**被钉住的时刻**, 不反映当前的评测成绩 —— 它是流水线回归测试, 不是质量指标。
免得下一个人拿它当最新数据读。

## 追加三: 报告里那一节别省

这一批的完成报告要有**"与 SPEC 不一致的地方, 逐条列出"**这一节。
上一批漏了 —— 内容其实报了 (冒烟子集不存在、step 挂不进 engine job),
但还有一条没提到:

> **冒烟 cassette 放在 `evals/cassettes/smoke/`, 而 SPEC-007 第五节写的是
> `apps/api/tests/cassettes/evals/`。**

**你放的位置更好** (评测资产归 evals, 不掺进 api 的测试夹具), 不用改 ——
SPEC 由评审方改。但它属于该逐条列出的偏离, 下次带上。

这一节不是形式主义: 前几周它让你正大光明地纠正过评审方三次
(illegal/repairable 那条张力、reject 判据、这次的 CI 根因),
省掉它等于把这条通道关掉。

---

## 顺序与花费

```
读 v1.3 那份 prompt
  → 73 条 clarify_answer 拆成按槽位的字典 (57 条新的 + 16 条 ambiguous 的)
  → 数据集升 v1.3, 判据一字节不动 (剥键回哈希自证)
  → L0 / L1 离线逐条重判确认同分, 不重跑
  → 重跑 L2 / C1 (约 ¥11)
  → 实跑冒烟验证 (追加一)
  → 冒烟 README 加重录规矩 (追加二)
  → 重生成 summary_ablation.md, 三代 (v1.1/v1.2/v1.3) 逐格对照
  → COST.md 收口
```

累计将到约 ¥55 / 批准 ¥120。**两臂在 ¥15 以内不用等确认**, 照例先 `--dry-run` 报一次。

C2 **不跑**, 按 v1.3 那份 prompt 第四节的处置: 从成功率表里撤出来,
另起一节只报"超时 7/20"与"输入输出比塌到 1.1:1"两个数。

---

## 老规矩

- **git 一律不执行**; `checkout` 不算只读。
- 数据集这轮授权改 v1.3, **只动 `clarify_answer` 的形状, 判据一个字节不许动**。
- 密钥只进 `.env`。
- v1.1 / v1.2 的归档与告示头全部保留 —— 三代对照本身是这一段最有说服力的东西。
