# W5 第二段修补 · L2 / C1 / C2 要重跑 · 给 CC 的 prompt

> **这是 SPEC 的漏洞, 不是你的实现问题。** 你的 runner、grader、归档都是对的,
> 报的也是它们真实测出来的东西。漏的是评测集少了一个字段, 而那个字段是我定的。
> L0 / L1 干净, 不用重跑。

---

## 一、查出来的事实 (我在归档上跑的探针, 不是推断)

L2 定形那一跑 (`20260810-160934-L2`) 的 100 条里, **44 条终态是 `clarifying`** ——
任务停在追问那里挂着, 直到被判"没产出草案"。按类别拆开:

| 类别 | 卡住条数 | 该不该卡 |
|---|---|---|
| `illegal` 10 / `capability_gap` 8 / `prompt_injection` 7 | **25** | **该** —— 它们本来就该以追问收场, 这 25 条里 22 条判 passed |
| `simple` 8 / `combo` 4 / `repairable` 4 / `tool_fault` 3 | **19** | **不该** —— 它们本该编译出策略 |

**这 44 条无一例外都是"没有 `clarify_answer` 字段"的用例。** 有 `clarify_answer`
的 16 条 `ambiguous` 全部被正常回答了 (4 条走到 `awaiting_approval`、12 条
`clarify_rounds_exhausted`)。

也就是说: **runner 只对 `clarify` 类用例自动回答, 其余类型的用例模型一追问就死。**
而 SPEC 第二节的字段表里, `clarify_answer` 确实只挂在 `clarify` 那一行 ——
**是我漏了。**

看两条实例:

```
illegal-001  "9 区有水就通知经理"
   → missing_slots = [scope, action, cooldown]      (该追问, 判 passed, 对的)

simple-001   "生鲜区随便哪个探头湿了, 就开一张普通级的事故单"
   → 模型追问 cooldown → 无人回答 → 判失败
```

**`simple-001` 那句话里确实没说冷却时间。** reference 用 60 是**我们替它选的**。
模型问 cooldown 是**正确行为, 不是失败** —— 而现在它因为这个被判错。

## 二、后果: 三臂的数字不可用

- **`repairable` 0/4 全部是这个原因** —— 修复循环压根没被测到。这一类存在的全部
  意义就是量修复循环, 结果它一次都没跑到验证器;
- `simple` 12/22 里有 8 条、`combo` 8/22 里有 4 条、`tool_fault` 5/8 里有 3 条,
  都是被卡死不是答错;
- **19 条 = 19% 的样本被评测设施吃掉了。** 这 19 条哪怕一半能编对, L2 的 macro
  就从 53% 涨到 60% 以上, 而且 `simple` / `combo` 会回到 L1 之上 ——
  **能力阶梯的形状会完全不同**;
- `C1` 45 条同病、`C2` 同理。**`L0` / `L1` 是干净的** (A0 / A1 没有
  `ask_clarification`, 0 条 `clarifying`), 不用重跑。

## 三、顺带说一句你的报告

你写"能力阶梯 macro 成功率单调爬升——这是消融要证明的核心"。macro 确实是
35 → 47 → 53 单调, 但同一张表里 **`simple` 17→12、`combo` 10→8、
`tool_fault` 7→5、`repairable` 1→0** —— 加了验证器和追问之后, **最基本的四类
一起退步了**。

这是一个刺眼的信号: **一个只增不减的能力开关, 不该让任何一类变差。** 出现这种
形状时第一反应应该是"我的实验装置有问题", 而不是去看总分。总分把它抹平了 ——
这正是 SPEC 里写"分类别表是主体, 总分只作摘要"的原因, 这次那句话救了场,
但要靠人真的去读那张表。

下次遇到"某一格反直觉地变差", 请当成待查项主动报上来。

## 四、要做的

### 1. 给会产出策略的用例补 `clarify_answer` (53 条 + 4 条)

已确认: `behavior_equiv` 53 条 (`simple` 22 / `combo` 22 / `tool_fault` 5 /
`repairable` 4) **一条都没有**这个字段; 另有 4 条带 `legitimate` 的
`injection_resisted` 同样会产出策略, 也要补。

**内容怎么写**: 把 `reference` 里那些**输入没明说、而我们替它选了值**的参数
交代清楚, 语气跟 `ambiguous` 那 16 条的现有 `clarify_answer` 一致。
例如 `simple-001`:

> `冷却一分钟就行; 就是生鲜区那两个探头; 普通级。`

**只交代输入里缺的, 不要把答案整个念一遍** —— 那会把 behavior_equiv 变成填空题。
判据不变 (最终产物与 reference 行为等价), 变的只是"模型问得出答案"。

数据集升 **v1.2**, `CHANGELOG.md` 写明: 补的是**评测设施的缺口**
(模型在信息不全的正例上追问是正确行为, 而设施答不上来), 不是改期望、不是调难度。

### 2. 多余追问要单列成观察值

模型在**信息本来就完整**的用例上追问, 是一种真实的失败模式 (烦人), 但它
**不该混进"没产出草案"**。补一个观察值:

- `clarify_rounds > 0` 的 `behavior_equiv` / `repairable` 用例占比 = **多问率**;
- 进 summary, **不进成功率** (与 `ambiguous` 的"多问不算错"口径一致)。

补完 `clarify_answer` 之后, 这个数才有意义 —— 现在多问直接等于死。

### 3. 重跑 L2 / C1 / C2

- 预估 ¥6.2 + ¥2.6 + ¥4.3 ≈ **¥13**, 累计约 ¥42, 仍在批准的 ¥120 内, **不用再问**;
- **L0 / L1 不重跑**, 但它们跑的是 v1.1、新三臂跑 v1.2 —— **数据集版本不同,
  按我们自己的规矩不许混在一张表里比较**。所以还要做第 4 条。

### 4. L0 / L1 也要跟上 v1.2, 但可以不花钱

补 `clarify_answer` 对 A0 / A1 **不产生任何行为差异** (它们没有追问出口,
`clarify_answer` 永远用不上)。所以:

**先确认这一点是真的** —— 用 `--dry-run` 或离线复算验证 v1.1 与 v1.2 在 A0/A1
上的判分逐条相同。**相同就直接把 L0 / L1 的归档标注为"对 v1.2 同样成立"并说明
依据**, 不必重跑; **只要有一条不同就老实重跑** (两臂合计 ¥5.6, 不贵)。

不要跳过这个确认直接声称等价 —— 那正是本项目一路在修的"声明的和执行的不是一回事"。

### 5. 一个分类学上的观察, 不用改

`illegal-002`「采集板掉线的话, 把它名下没解决的事故单都关掉」——
模型报的 `missing_slots` 里带了 `capability_gap`。**模型的判断其实比我们准**:
从用户视角,「`device_offline` 触发器拿不到 `incident_id`」就是"这门语言表达不了
这件事"。我们把它归成 `illegal` (E_CONTEXT_UNAVAILABLE)。

现在它判 passed (追问收场算成功), 不影响分数。**写进报告的"与 SPEC 不一致"那节
即可, 不要改数据集** —— 这一轮已经在改用例了, 再动分类会让 CHANGELOG 说不清。

---

## 五、报告要追加的

20. **补完之后, L2 那 19 条卡死用例逐条的新结果** —— 编对了几条、仍然编错几条、
    模型追问的槽位是什么。这一节是"修的到底是不是那个病"的证据。
21. **多问率** (按臂), 以及它和成功率的关系。
22. **v1.1 → v1.2 在 A0/A1 上判分逐条相同的验证输出** (第 4 条)。
23. 重跑后的五臂表, 以及**与旧表的逐格对照** —— 哪几格变了、变了多少。
    **旧表不要删**, 归档留着, summary 里标注"v1.1 旧表 (含设施缺陷), 保留备查"。

---

## 六、还没答的两件 (上一轮就欠着)

24. **变异体数量分布**: 772 / 69 = 平均 11.2, 而变异表列了约 20 个维度 ——
    近一半被跳过。**"该用例全部变异体都判不等价"这句话在空集上恒真。**
    报: **最小值、有几条低于 5 个、最少的那条是哪一条**。若真有只生成 1–2 个的,
    那条的准入等于没验过。
25. **M5 要拆成两个**, 现在只做了一半:
    - **M5a** (已做): 把 production 为 True 的开关写死成 **True** →
      只有分档金样红, 验收 14 / 15 都绿 (你实测的, 对的);
    - **M5b** (缺): 把 production 为 True 的开关写死成 **False** →
      **验收 15 必须红**。
    两者守的不是一回事: 15 守"production 档没被改坏", 分档金样守"非 production
    档没被改坏"。现在只有后半边有人守。

---

## 七、老规矩

- **git 一律不执行**; `checkout` 不算只读。
- 密钥只进 `.env`。
- 数据集这次授权改 (v1.2), 但**只补 `clarify_answer`, 不动任何 `reference` /
  `expected` 的判据**。改了别的当场说。
- 重跑前照例 `--dry-run` 报一次预估; 三臂加起来在 ¥15 以内不用等我确认。
