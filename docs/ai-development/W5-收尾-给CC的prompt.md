# W5 收尾 · 给 CC 的 prompt

> 第三批已复核通过, 报告里的数字评审方全部独立重算过, 逐格对上。这一批是 **W5 最后一批**:
> 四件小事, **零真实调用、零花费、不重跑任何臂**。
>
> 两件待拍板的 (C1 注入门槛口径 / 兜底话改不改) **不在这一批**, 你不要动。

---

## 一、给探针那一跑加告示头

`evals/runs/20260811-003029-L2/` 是 `sample_size=2` 的开跑前探针 (¥0.16),
但它的 `summary.md` 开头没有任何说明。翻归档的人会把它读成"一次只有 2 条、
成功率 100% 的 L2"。

按 v1.1/v1.2 作废归档那几份的体例, 在 `summary.md` 顶上加告示头, 写明:
**这是澄清应答改造后的开跑前探针, 2 条用例 (`ambig-001` / `repairable-001`),
用途是确认按槽位应答这条路接上了, 不是一次评测结果, 不要引用它的任何数字。**

`manifest.json` 里若有内容校验和, 一并更新; 若没有, 说明一句"没有"。

## 二、`summary_ablation.md` 第 6 节的表补齐

正文写"逐条查了 L2 那 11 条耗尽三轮的用例", 底下的表只有 8 行。
读者会数, 数出来对不上。补齐缺的三条: `ambig-014`、`combo-022`、`fault-004`。

`fault-004` 那条**必须在表里**: 它是 11 条里唯一一条**耗尽三轮却仍判通过**的,
它证明"耗尽三轮"与"失败"不是同一件事 —— 恰好是这一节要讲的分辨力。

## 三、`blind_answers` 补一条会翻转的测试

第三批报告第四节自己写了: 这个计数器**没有任何断言守着**, 而"它坏了"的表现
恰恰是**永远为 0**, 与"从来没降级过"长得一模一样 —— 本项目栽过五次的那个形状。

补一条: 打桩一个**恒返回空槽位**的 `slots_reader`, 跑一条会进澄清的用例,
断言 `blind_answers` 涨到 1。

**做完自己做一次变异测试**: 把累加那一行删掉, 确认这条测试会红。
红了再交, 报告里写明变异测了什么、结果如何。

## 四、`--cases-file` 的两条错误路径补测试

第三批报告第四节第 7 条列的两条没人守的路径:

- 清单里出现数据集中**不存在的 id** → 当场 `parser.error` 中止;
- 清单**为空** → 中止。

各补一条测试。理由与冒烟门槛同源: **少跑几条的冒烟照样是绿的, 而它守的东西已经没了。**

---

## 老规矩

- **git 一律不执行**; `checkout` 不算只读; 只读命令加 `--no-optional-locks`。
- 不跑任何真实模型调用, 不改数据集, 不改判据。
- 完成报告写进 `docs/ai-development/W5-收尾-完成报告.md`, **三节照旧**:
  与 SPEC 不一致的地方 (含你认为评审方写错的) / 新增分支各由哪条测试守着 /
  报数字必须带产生它的配置。**不要只打在终端里。**
