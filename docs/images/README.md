这个目录放 README 引用的界面素材。

现有四张, 与 README "它做什么"一节一一对应:

- studio-trace.gif      整条时间线一步步长出来 (录屏, 无加速)
- studio-clarify.png    Agent 主动追问的那两条 (#8 Agent 问 / #9 发起人答)
- studio-approval.png   回放报告与审批区 (含自批被拦的红字)
- studio-inbox.png      任务列表: 没走完的排在最前, 顶上写着有几条在等审批

约定:

- 截图用 2 倍图 (macOS Cmd+Shift+4 默认就是), 宽度不必压缩 —— GitHub 会自适应;
- 只截需要的那一块, 不要整窗口截图 (浏览器地址栏、书签栏都是噪音);
- 换素材直接覆盖同名文件, 不要改 README 里的路径, 否则历史链接会断。
